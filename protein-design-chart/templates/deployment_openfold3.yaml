apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "protein-design-chart.fullname" . }}-openfold3
  labels:
    {{- include "protein-design-chart.labels" . | nindent 4 }}
    app.kubernetes.io/component: openfold3
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "protein-design-chart.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: openfold3
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "protein-design-chart.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: openfold3
    spec:
      {{- if .Values.imagePullSecret }}
      imagePullSecrets:
        - name: {{ .Values.imagePullSecret.name }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      initContainers:
        - name: volume-permissions
          image: busybox:1.31.1
          command:
            - sh
            - -c
            - |
              mkdir -p {{ .Values.openfold3.cacheFolder | quote }} && 
              chmod -R 777 {{ .Values.openfold3.cacheFolder | quote }}
          volumeMounts:
            - name: openfold3-pv1
              mountPath: {{ .Values.openfold3.cacheFolder | quote }}
          securityContext:
            runAsUser: 0
      containers:
        - name: {{ .Values.openfold3.containerName }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.openfold3.repository }}:{{ .Values.openfold3.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.openfold3.pullPolicy }}
          env:
            - name: NGC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.imagePullSecret.secretName }}
                  key: {{ .Values.imagePullSecret.secretKey }}
          volumeMounts:
            - mountPath: {{ .Values.openfold3.cacheFolder | quote }}
              name: openfold3-pv1
            - mountPath: /dev/shm
              name: dshm
          ports:
            - name: http
              containerPort: {{ .Values.openfold3.containerPort }}
              protocol: TCP
          livenessProbe:
            initialDelaySeconds: 600
            httpGet:
              path: /v1/health/live
              port: {{ .Values.openfold3.containerPort }}
          readinessProbe:
            initialDelaySeconds: 600
            httpGet:
              path: /v1/health/ready
              port: {{ .Values.openfold3.containerPort }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
        - name: openfold3-pv1
          persistentVolumeClaim:
            claimName: protein-design-shared-vol
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 16Gi
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
